[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]

# (Optional) Force SPA-style routing so deep links work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
